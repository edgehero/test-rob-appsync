type Mutation {
  putPost(title: String!): Post
}

type Query {
  post(id: ID!): Post
}

type Post {
  id: ID!
  type: String!
  title: String!
  updatedAt: String!
}

schema {
  query: Query
  mutation: Mutation
}
